services:
  - type: web
    name: stage-outfit-match-agent
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: LLM_PROVIDER
        value: mock
      - key: ALLOWED_ORIGINS
        value: "*"
